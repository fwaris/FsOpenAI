namespace FsOpenAI.TravelSurvey
open FsOpenAI.Shared

[<RequireQualifiedAccess>]
module QnA =

    let genPlan parms invCtx ch dispatch = ()
        
    let run parms invCtx ch (codeGen:CodeEvalParms) dispatch = ()

